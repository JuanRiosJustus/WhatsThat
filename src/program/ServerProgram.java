package program;

import javafx.application.Application;
import dialogs.ServerDialog;

public class ServerProgram {
	public static void main(String[] args) {
		Application.launch(ServerDialog.class, args);
	}
}